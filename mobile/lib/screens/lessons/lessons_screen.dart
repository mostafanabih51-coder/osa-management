import 'package:flutter/material.dart';
import '../../services/api_service.dart';

class LessonsScreen extends StatefulWidget{const LessonsScreen({super.key});@override State<LessonsScreen> createState()=>_LessonsScreenState();}
class _LessonsScreenState extends State<LessonsScreen>{
 List lessons=[],teachers=[],students=[],supervisors=[],groups=[];List<String> subjects=[];bool loading=true;static const fallback=['العربية','اللغة الإنجليزية','الرياضيات','العلوم','الدراسات الاجتماعية','Math','Science','English','Arabic','German','French','Spanish','Quran'];
 List<dynamic> list(dynamic x)=>x is List?List<dynamic>.from(x):(x is Map&&x['data'] is List?List<dynamic>.from(x['data']):[]);int id(dynamic x)=>int.tryParse('${x['id']}')??0;String sub(dynamic x)=>x is Map?'${x['subject']??x['name']??''}':'$x';void msg(Object e)=>ScaffoldMessenger.of(context).showSnackBar(SnackBar(content:Text(e.toString().replaceFirst('Exception: ',''))));
 @override void initState(){super.initState();load();}
 Future<void> load()async{try{final r=await Future.wait([ApiService.get('lessons'),ApiService.get('teachers'),ApiService.get('students'),ApiService.get('supervisors'),ApiService.get('groups'),ApiService.get('subjects')]);final ss=list(r[5]).map(sub).where((x)=>x.isNotEmpty).toSet().toList();if(mounted)setState((){lessons=list(r[0]);teachers=list(r[1]);students=list(r[2]);supervisors=list(r[3]);groups=list(r[4]);subjects=ss.isEmpty?fallback:ss;loading=false;});}catch(e){if(mounted){setState(()=>loading=false);msg(e);}}}
 List<String> studentSubjects(dynamic s){final x=list(s['subjects']).map(sub).where((v)=>v.isNotEmpty).toSet().toList();return x.isEmpty?subjects:x;}
 List<dynamic> studentTeachers(dynamic s,String subject){final x=list(s['teachers']).where((t){final p=t is Map?t['pivot']:null;return p is Map&&p['subject']!=null?'${p['subject']}'==subject:true;}).toList();return x.isEmpty?teachers:x;}
 Future<void> form({String? initialType,dynamic lesson})async{if(teachers.isEmpty){msg(Exception('أضف مدرسًا أولًا.'));return;}String type='${lesson?['type']??initialType??'private'}';dynamic student=students.isEmpty?null:students.first;dynamic group=groups.isEmpty?null:groups.first;if(lesson!=null){final sid=id(lesson['student']??{}),gid=id(lesson['group']??{});if(sid>0)student=students.firstWhere((s)=>id(s)==sid,orElse:()=>student);if(gid>0)group=groups.firstWhere((g)=>id(g)==gid,orElse:()=>group);}if(type=='private'&&student==null){msg(Exception('أضف طالبًا أولًا.'));return;}if(type=='group'&&group==null){msg(Exception('أنشئ مجموعة أولًا.'));return;}String subject='${lesson?['subject']??(type=='group'?'${group['subject']??subjects.first}':studentSubjects(student).first)}';if(type=='private'&&!studentSubjects(student).contains(subject))subject=studentSubjects(student).first;int teacherId=id(lesson?['teacher']??(type=='group'?group['teacher']:studentTeachers(student,subject).first));if(teacherId==0)teacherId=id(teachers.first);int? supervisorId=lesson?['supervisor'] is Map?id(lesson['supervisor']):int.tryParse('${lesson?['supervisor_id']??''}');final rate=TextEditingController(text:'${lesson?['teacher_rate']??(type=='group'?group['teacher_rate']??'':'')}'),starts=TextEditingController(text:'${lesson?['starts_at']??''}'),ends=TextEditingController(text:'${lesson?['ends_at']??''}');final key=GlobalKey<FormState>();final ok=await showDialog<bool>(context:context,builder:(dc)=>StatefulBuilder(builder:(c,set)=>AlertDialog(title:Text(lesson==null?(type=='group'?'إضافة حصة جروب':'إضافة حصة خاصة'):'تعديل الحصة'),content:SizedBox(width:520,child:SingleChildScrollView(child:Form(key:key,child:Column(crossAxisAlignment:CrossAxisAlignment.stretch,children:[DropdownButtonFormField<String>(value:type,decoration:const InputDecoration(labelText:'نوع الحصة'),items:const[DropdownMenuItem(value:'private',child:Text('حصة خاصة')),DropdownMenuItem(value:'group',child:Text('حصة جروب'))],onChanged:lesson!=null?null:(v){if(v==null)return;set((){type=v;if(type=='private'){student=students.first;subject=studentSubjects(student).first;teacherId=id(studentTeachers(student,subject).first);}else{group=groups.first;subject='${group['subject']??subjects.first}';teacherId=id(group['teacher']);rate.text='${group['teacher_rate']??''}';}});}),if(type=='private')DropdownButtonFormField<int>(value:id(student),decoration:const InputDecoration(labelText:'الطالب *'),items:students.map((s)=>DropdownMenuItem<int>(value:id(s),child:Text('${s['name']}'))).toList(),onChanged:(v){if(v!=null)set((){student=students.firstWhere((s)=>id(s)==v);subject=studentSubjects(student).first;teacherId=id(studentTeachers(student,subject).first);});}),if(type=='group')DropdownButtonFormField<int>(value:id(group),decoration:const InputDecoration(labelText:'المجموعة *'),items:groups.map((g)=>DropdownMenuItem<int>(value:id(g),child:Text('${g['name']}'))).toList(),onChanged:(v){if(v!=null)set((){group=groups.firstWhere((g)=>id(g)==v);subject='${group['subject']??subjects.first}';teacherId=id(group['teacher']);rate.text='${group['teacher_rate']??''}';});}),DropdownButtonFormField<int>(value:teacherId,decoration:const InputDecoration(labelText:'المدرس *'),items:(type=='group'?[group['teacher']??teachers.first]:studentTeachers(student,subject)).map((t)=>DropdownMenuItem<int>(value:id(t),child:Text('${t['name']}'))).toList(),onChanged:type=='group'?null:(v){if(v!=null)set(()=>teacherId=v);}),DropdownButtonFormField<String>(value:subject,decoration:const InputDecoration(labelText:'المادة *'),items:(type=='group'?[subject]:studentSubjects(student)).map((s)=>DropdownMenuItem<String>(value:s,child:Text(s))).toList(),onChanged:type=='group'?null:(v){if(v!=null)set(()=>subject=v);}),if(supervisors.isNotEmpty)DropdownButtonFormField<int?>(value:supervisorId,decoration:const InputDecoration(labelText:'المشرف'),items:[const DropdownMenuItem<int?>(value:null,child:Text('بدون مشرف')),...supervisors.map((s)=>DropdownMenuItem<int?>(value:id(s),child:Text('${s['name']}')))],onChanged:(v)=>set(()=>supervisorId=v)),TextFormField(controller:rate,keyboardType:TextInputType.number,decoration:const InputDecoration(labelText:'سعر الحصة للمدرس *'),validator:(v)=>double.tryParse(v??'')==null?'أدخل سعرًا صحيحًا':null),TextFormField(controller:starts,decoration:const InputDecoration(labelText:'البداية YYYY-MM-DD HH:MM'),validator:(v)=>v==null||v.trim().isEmpty?'مطلوب':null),TextFormField(controller:ends,decoration:const InputDecoration(labelText:'النهاية YYYY-MM-DD HH:MM'),validator:(v)=>v==null||v.trim().isEmpty?'مطلوب':null)]))),actions:[TextButton(onPressed:()=>Navigator.pop(dc,false),child:const Text('إلغاء')),FilledButton(onPressed:(){if(key.currentState!.validate())Navigator.pop(dc,true);},child:const Text('حفظ'))]));if(ok==true){try{final body={'type':type,'teacher_id':teacherId,'supervisor_id':supervisorId,'group_id':type=='group'?id(group):null,'student_id':type=='private'?id(student):null,'subject':subject,'starts_at':starts.text.trim(),'ends_at':ends.text.trim(),'teacher_rate':double.parse(rate.text),'status':lesson?['status']??'scheduled'};if(lesson==null)await ApiService.post('lessons',body);else await ApiService.put('lessons/${id(lesson)}',body);await load();}catch(e){msg(e);}}for(final c in[rate,starts,ends])c.dispose();}
 Future<void> remove(dynamic lesson)async{final ok=await showDialog<bool>(context:context,builder:(dc)=>AlertDialog(title:const Text('حذف الحصة'),content:const Text('هل تريد حذف هذه الحصة؟ سيتم حذف المستحقات المرتبطة بها.'),actions:[TextButton(onPressed:()=>Navigator.pop(dc,false),child:const Text('إلغاء')),FilledButton(onPressed:()=>Navigator.pop(dc,true),child:const Text('حذف'))]));if(ok==true){try{await ApiService.delete('lessons/${id(lesson)}');await load();}catch(e){msg(e);}}}
 @override Widget build(BuildContext context)=>Scaffold(appBar:AppBar(title:const Text('الدروس والحصص'),actions:[IconButton(onPressed:load,icon:const Icon(Icons.refresh))]),body:loading?const Center(child:CircularProgressIndicator()):RefreshIndicator(onRefresh:load,child:ListView(padding:const EdgeInsets.fromLTRB(16,12,16,24),children:[Card(child:Padding(padding:const EdgeInsets.all(14),child:const Text('الحصة الخاصة تحتاج طالبًا ومدرسًا مرتبطًا بالمادة. حصة الجروب تستخدم مدرس ومادة المجموعة. يمكنك تعديل أو حذف أي حصة.'))),Row(children:[Expanded(child:FilledButton.icon(onPressed:()=>form(initialType:'private'),icon:const Icon(Icons.person),label:const Text('حصة خاصة'))),const SizedBox(width:10),Expanded(child:FilledButton.icon(onPressed:()=>form(initialType:'group'),icon:const Icon(Icons.groups),label:const Text('حصة جروب')))]),const SizedBox(height:12),Text('الحصص (${lessons.length})',style:const TextStyle(fontWeight:FontWeight.bold,fontSize:17)),if(lessons.isEmpty)const Padding(padding:EdgeInsets.all(25),child:Center(child:Text('لا توجد حصص بعد.'))),...lessons.map((l){final t=l['teacher'],s=l['student'],g=l['group'];return Card(child:ListTile(leading:CircleAvatar(child:Icon(l['type']=='group'?Icons.groups:Icons.person)),title:Text('${l['subject']??''}',style:const TextStyle(fontWeight:FontWeight.bold)),subtitle:Text('${l['type']=='group'?'جروب':'خاصة'} • المدرس: ${t?['name']??''}\n${l['type']=='group'?'المجموعة: ${g?['name']??''}':'الطالب: ${s?['name']??''}'}\n${l['starts_at']??''}'),isThreeLine:true,trailing:Wrap(mainAxisSize:MainAxisSize.min,children:[IconButton(onPressed:()=>form(lesson:l),icon:const Icon(Icons.edit)),IconButton(onPressed:()=>remove(l),icon:const Icon(Icons.delete))]))})])));
}
